{
  "name": "@test/script-hooks",
  "scripts": {
    "pretest": "echo pretest",
    "test": "echo test",
    "posttest": "echo posttest",
    "build": "echo build",
    "prebuild": "echo prebuild"
  }
}
