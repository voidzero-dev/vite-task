# Tests that TaskConfig tasks do NOT get pre/post hooks expanded,
# even if matching pre/post package.json scripts exist.
# `test` is a TaskConfig; `pretest` is a PackageJsonScript.
# Running `test` must NOT expand `pretest` as a hook.

[[plan]]
name = "task_config_test_does_not_expand_pretest_hook"
args = ["run", "test"]
