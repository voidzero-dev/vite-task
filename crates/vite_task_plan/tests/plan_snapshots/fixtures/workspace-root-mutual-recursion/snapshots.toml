# Tests mutual recursion detection: root#build → vp run -r test → root#test → vp run -r build → root#build
# This is NOT self-recursion — it's caught by check_recursion as mutual recursion.

[[plan]]
name = "mutual recursion error"
args = ["run", "-r", "build"]
