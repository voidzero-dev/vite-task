# Tests that different extra args get separate cache entries

[[e2e]]
name = "individual cache for extra args"
steps = [
  "vp run --verbose say a # cache miss",
  "vp run --verbose say b # cache miss, different args",
  "vp run --verbose say a # cache hit",
  "vp run --verbose say b # cache hit",
]
