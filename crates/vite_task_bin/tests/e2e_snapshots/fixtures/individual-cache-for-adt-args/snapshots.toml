# Tests that different extra args get separate cache entries

[[e2e]]
name = "individual cache for extra args"
steps = [
  "vp run --details say a # cache miss",
  "vp run --details say b # cache miss, different args",
  "vp run --details say a # cache hit",
  "vp run --details say b # cache hit",
]
