# Tests that .git directory is ignored by lint

[[e2e]]
name = "lint dot git"
steps = [
  "mkdir .git",
  "vp run lint # cache miss",
  "echo hello > .git/foo.txt # add file inside .git",
  "vp run lint # cache hit, .git is ignored",
]
