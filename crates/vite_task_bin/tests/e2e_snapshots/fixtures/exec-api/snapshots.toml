[[e2e]]
name = "exec not triggered from script"
steps = [
  "FOO=bar vp run lint-task # no print-env FOO output",
]

[[e2e]]
name = "exec caching"
steps = [
  "FOO=bar vp lint # cache miss",
  "FOO=bar vp lint # cache hit, silent",
  "FOO=baz vp lint # env changed, cache miss",
]
