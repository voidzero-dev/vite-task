# Tests lint caching with file changes

[[e2e]]
name = "direct lint"
steps = [
  "vp run --details lint # cache miss",
  "echo debugger > main.js # add lint error",
  "vp run --details lint # cache miss, lint fails",
]
