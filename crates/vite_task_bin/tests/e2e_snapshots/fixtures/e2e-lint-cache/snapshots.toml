# Tests lint caching with file changes

[[e2e]]
name = "direct lint"
steps = [
  "vp run --verbose lint # cache miss",
  "echo debugger > main.js # add lint error",
  "vp run --verbose lint # cache miss, lint fails",
]
