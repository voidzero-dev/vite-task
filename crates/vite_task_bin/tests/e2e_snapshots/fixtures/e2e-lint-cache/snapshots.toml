# Tests synthetic task caching with file changes

[[e2e]]
name = "direct lint"
steps = [
  "vt run lint # cache miss",
  "echo modified > main.js # modify tracked file",
  "vt run lint # cache miss, file changed",
]
