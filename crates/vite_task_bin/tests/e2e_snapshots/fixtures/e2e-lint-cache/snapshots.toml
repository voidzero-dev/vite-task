# Tests lint caching with file changes

[[e2e]]
name = "direct lint"
steps = [
  "vite run lint # cache miss",
  "echo debugger > main.js # add lint error",
  "vite run lint # cache miss, lint fails",
]
