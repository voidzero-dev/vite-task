# Tests that tasks with identical commands share cache entries

[[e2e]]
name = "shared caching inputs"
steps = [
  "vp run --details script1 # cache miss",
  "vp run --details script2 # cache hit, same command as script1",
  "replace-file-content foo.txt initial modified # modify shared input",
  "vp run --details script2 # cache miss, input changed",
  "vp run --details script1 # cache hit, script2 already warmed cache",
]
