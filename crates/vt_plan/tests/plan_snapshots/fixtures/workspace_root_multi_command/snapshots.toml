# Tests multi-command with self-referencing: root build = "echo pre && vt run -r build"
# The skip rule skips the recursive `vt run -r build` part, echo pre still runs.

[[plan]]
name = "multi_command_skips_recursive_part"
args = ["run", "-r", "build"]
compact = true
