{
  "name": "@test/remote-cache-config",
  "scripts": {
    "inherit": "vt run build",
    "env-off": "VP_REMOTE_CACHE=off vt run build",
    "cli-read": "VP_REMOTE_CACHE=off vt run --remote-cache=read build",
    "siblings": "VP_REMOTE_CACHE=off vt run build && vt run build",
    "endpoints": "VP_REMOTE_CACHE_URL=https://other.example/projects/nested vt run build && vt run build",
    "prefix": "VP_REMOTE_CACHE=off VP_REMOTE_CACHE_URL=https://other.example vtt print-file package.json"
  }
}
