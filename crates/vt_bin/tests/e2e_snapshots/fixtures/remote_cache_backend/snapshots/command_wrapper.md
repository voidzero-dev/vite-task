# command_wrapper

## `remote-cache-server node -e 'console.log(new URL(process.env.VP_REMOTE_CACHE_URL).pathname)'`

The endpoint includes a namespace base path.

```
/projects/test
```

## `remote-cache-server vtt exit 3`

The wrapper exits with the command's exit code.

**Exit code:** 3

```
```
