{
  "name": "@test/api",
  "version": "1.0.0",
  "scripts": {
    "build": "echo Generate schemas && echo Compile TypeScript && echo Bundle API && echo Copy assets",
    "test": "echo Testing API",
    "start": "echo Starting API server",
    "dev": "echo Watch mode && echo Start dev server"
  },
  "dependencies": {
    "@test/shared": "workspace:*",
    "@test/config": "workspace:*"
  }
}
